# Git Hands-on Lab 5: Remote Cleanup and Push

## Objectives
- Verify clean status of master branch.
- List local and remote branches.
- Synchronize remote repository (`git pull origin master`).
- Push local commits to remote repository (`git push origin master`).

## Commands Executed
```bash
git status
git branch -a
git pull origin master
git push origin master
```
